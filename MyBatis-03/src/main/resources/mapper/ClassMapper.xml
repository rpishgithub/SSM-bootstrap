<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.rpish.mapper.ClassMapper">

    <select id="listStudentsInClass" parameterType="Integer" resultMap="StudentsInClassResult">
        select c.id cid,c.name cname,s.id sid,s.name sname,s.sex ssex
        from students s,class c
        where s.class_id = c.id and c.id = #{id}
    </select>

    <resultMap id="StudentsInClassResult" type="tech.rpish.po.Class">
        <id property="id" column="cid"/>
        <result property="name" column="cname"/>
        <collection property="studentList" ofType="tech.rpish.po.Students">
            <id property="id" column="sid"/>
            <result property="name" column="sname"/>
            <result property="sex" column="ssex"/>
        </collection>
    </resultMap>

</mapper>